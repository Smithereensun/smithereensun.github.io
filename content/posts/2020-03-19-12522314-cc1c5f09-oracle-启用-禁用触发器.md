{

  "title": "Oracle 启用、禁用触发器",
  "date": "2020-03-19",
  "description": "基本语法 启用指定表中所有触发器(trigger) 禁用指定表中所有触发器(trigger) 启用指定触发器(trigger) 禁用指定触发器(trigger)",
  "tags": [
    "Oracle"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/12522314.html"

}

# 基本语法

## 启用指定表中所有触发器(trigger)

```text
alter table table_name enable all triggers;

语法：
alter table 表名 enable all triggers;
```

## 禁用指定表中所有触发器(trigger)

```text
alter table table_name disable all triggers;

语法：
alter table 表名 disable all triggers;
```

## 启用指定触发器(trigger)

```text
alter trigger trigger_name enable;

语法：
alter trigger 触发器名 enable;
```

## 禁用指定触发器(trigger)

```text
alter trigger trigger_name disable;

语法：
alter trigger 触发器名 disable;
```
