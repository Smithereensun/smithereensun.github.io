{

  "title": "IDEA+Maven+Spring5.X项目创建",
  "date": "2020-07-14",
  "description": "创建maven 添加依赖 pom.xml 添加配置文件applicationContext.xml 在src/main/resources下创建applicationContext.xml bean标签 id属性：指定Bean的名称，在Bean被别的类依赖时使用 name属性：用于指定Bean的别名",
  "tags": [
    "Spring"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/13302036.html"

}

# 创建maven

![](/imported/posts/2020-07-14-13302036-b4d58d10-idea-maven-spring5-x项目创建/images/img_001_8fc356675027.png)

![](/imported/posts/2020-07-14-13302036-b4d58d10-idea-maven-spring5-x项目创建/images/img_002_78b41de5aaef.png)

# 添加依赖

pom.xml

```text
    <dependencies>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
            <version>5.2.5.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>5.2.5.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>5.2.5.RELEASE</version>
        </dependency>
    </dependencies>
```

# 添加配置文件applicationContext.xml

在src/main/resources下创建applicationContext.xml

```text
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">
    <bean id="video" class="net.cybclass.sp.domain.Video">
        <property name="id" value="9"></property>
        <property name="title" value="Spring5.X课程"></property>
    </bean>
</beans>
```

bean标签

- id属性：指定Bean的名称，在Bean被别的类依赖时使用
- name属性：用于指定Bean的别名，如果没有id，也可以用name
- class属性：用于指定Bean的来源，要创建Bean的class类，需要全限定名

# 创建Video.java

```text
package net.cybclass.sp.domain;

public class Video {
    private int id;
    private String title;

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public String getTitle() {
        return title;
    }

    public void setTitle(String title) {
        this.title = title;
    }
}
```

# 创建app.java

```text
package net.cybclass.sp;

import net.cybclass.sp.domain.Video;
import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;

public class app {
    public static void main(String[] args) {
        ApplicationContext applicationContext=new ClassPathXmlApplicationContext("applicationContext.xml");
        Video video=(Video) applicationContext.getBean("video");
        System.out.println(video.getTitle());
    }
}
```

# 完整项目结构

![](/imported/posts/2020-07-14-13302036-b4d58d10-idea-maven-spring5-x项目创建/images/img_003_a38f48aa72ae.png)

# 运行

![](/imported/posts/2020-07-14-13302036-b4d58d10-idea-maven-spring5-x项目创建/images/img_004_c13a9c81e5ac.gif)
