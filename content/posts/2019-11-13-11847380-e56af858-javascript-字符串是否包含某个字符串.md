{

  "title": "JavaScript 字符串是否包含某个字符串",
  "date": "2019-11-13",
  "description": "字符串方式 indexOf() (推荐) 注：indexOf() 方法可返回某个指定的字符串值在字符串中首次出现的位置。如果要检索的字符串值没有出现，则该方法返回 -1。 search() 注：search() 方法用于检索字符串中指定的子字符串，或检索与正则表达式相匹配的子字符串。如果没有找到任何",
  "tags": [
    "JavaScript"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/11847380.html"

}

# 字符串方式

##  indexOf()   (推荐)

```text
var str = "123";
console.log(str.indexOf("3") != -1 );  // true
```

注：indexOf() 方法可返回某个指定的字符串值在字符串中首次出现的位置。如果要检索的字符串值没有出现，则该方法返回 -1。

## search() 

```text
var str = "123";
console.log(str.search("3") != -1 );  // true
```

注：search() 方法用于检索字符串中指定的子字符串，或检索与正则表达式相匹配的子字符串。如果没有找到任何匹配的子串，则返回 -1。

## match()

```text
var str = "123";
var reg = RegExp(/3/);
if(str.match(reg)){
    // 包含
}
```

注：match() 方法可在字符串内检索指定的值，或找到一个或多个正则表达式的匹配。

# 正则表达式

## test() 

```text
var str = "123";
var reg = RegExp(/3/);
console.log(reg.test(str)); // true
```

注：test() 方法用于检索字符串中指定的值。返回 true 或 false。

## exec()

```text
var str = "123";
var reg = RegExp(/3/);
if(reg.exec(str)){
    // 包含
}
```

注：exec() 方法用于检索字符串中的正则表达式的匹配。返回一个数组，其中存放匹配的结果。如果未找到匹配，则返回值为 null。
