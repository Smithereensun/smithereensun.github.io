{

  "title": "JavaSE进阶核心之class类",
  "date": "2020-08-02",
  "description": "Java顶级对象之Object 什么是Object类 Object类位于java.lang包中，java.lang包包含着Java最基础和核心的类，在编译时会自动导入 Object类是所有java类的祖先，每个类都使用Object作为超类 常见方法 native方法：本地方法，具体是用C(C++)在",
  "tags": [
    "Java"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/13417358.html"

}

# Java顶级对象之Object

## 什么是Object类

- Object类位于java.lang包中，java.lang包包含着Java最基础和核心的类，在编译时会自动导入
- Object类是所有java类的祖先，每个类都使用Object作为超类

## 常见方法

```text
public final native Class<?> getClass()
讲解：获取对象的运⾏时class对象，class对象就是描述对象所属类的对象, 类的对象可以获取这个
类的基本信息，如名、包、字段、⽅法等
public native int hashCode()
讲解：获取对象的散列值，集合框架中应⽤，去除，⽐如HashMap
public boolean equals(Object obj)
讲解：⽐较两个对象，如果这两个对象引⽤指向的是同⼀个对象，那么返回true，否则返回false
集合框架中有讲
public String toString()
讲解：⽤于返回⼀个可代表对象的字符串，看源码可以得知，默认返回格式如下：对象的class名称 +
@ + hashCode的⼗六进制字符串,所以前⾯课程写对象时候，需要重写这个⽅法，⽅便调试
```

![](/imported/posts/2020-08-02-13417358-d8b0cd1e-javase进阶核心之class类/images/img_001_20214fcb1c0b.png)

native方法：本地方法，具体是用C(C++)在DLL中实现的，然后通过JNI调用

什么是JNI：Java平台和本地C(C++)代码进行互操作的API，称为Java Native Interface(Java本地接口)

反射学习：[点我直达](https://www.cnblogs.com/chenyanbin/p/13030159.html)
