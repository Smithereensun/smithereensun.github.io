{

  "title": "Oracle ROW_NUMBER() OVER()函数用法详解 （分组排序 例子多）",
  "date": "2020-05-29",
  "description": "转载☞：https://blog.csdn.net/qq_25221835/article/details/82762416 ROW_NUMBER 语法 解释 row_number() over()分组排序功能： 在使用 row_number() over()函数时候，over()里头的分组以及排序",
  "tags": [
    "Oracle"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/12985658.html"

}

转载☞：https://blog.csdn.net/qq_25221835/article/details/82762416

# ROW_NUMBER

## 语法

```text
语法格式：row_number() over(partition by 分组列 order by 排序列 desc)
```

## 解释

row_number() over()分组排序功能：

在使用 row_number() over()函数时候，over()里头的分组以及排序的执行晚于 where 、group by、  order by 的执行。

## 示例1

表数据：

```text
create table TEST_ROW_NUMBER_OVER(
       id varchar(10) not null,
       name varchar(10) null,
       age varchar(10) null,
       salary int null
);
select * from TEST_ROW_NUMBER_OVER t;

insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(1,'a',10,8000);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(1,'a2',11,6500);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(2,'b',12,13000);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(2,'b2',13,4500);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(3,'c',14,3000);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(3,'c2',15,20000);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(4,'d',16,30000);
insert into TEST_ROW_NUMBER_OVER(id,name,age,salary) values(5,'d2',17,1800);
```

### 依次排序：对查询结果进行排序（无分组）

```text
select id,name,age,salary,row_number()over(order by salary desc) rn
from TEST_ROW_NUMBER_OVER t
```

结果

![](/imported/posts/2020-05-29-12985658-256c6c98-oracle-row-number-over-函数用法详解-分组排序-例子多/images/img_001_b43163e847f8.png)

### 根据id分组排序

```text
select id,name,age,salary,row_number()over(partition by id order by salary desc) rank
from TEST_ROW_NUMBER_OVER t
```

结果

![](/imported/posts/2020-05-29-12985658-256c6c98-oracle-row-number-over-函数用法详解-分组排序-例子多/images/img_002_26a7b596e1d6.png)

### 找出每一组中序号为一的数据

```text
select * from(select id,name,age,salary,row_number()over(partition by id order by salary desc) rank
from TEST_ROW_NUMBER_OVER t)
where rank <2
```

结果

![](/imported/posts/2020-05-29-12985658-256c6c98-oracle-row-number-over-函数用法详解-分组排序-例子多/images/img_003_63b46c1c8a39.png)

### 排序找出年龄在13岁到16岁数据，按salary排序

```text
select id,name,age,salary,row_number()over(order by salary desc)  rank
from TEST_ROW_NUMBER_OVER t where age between '13' and '16'
```

结果：结果中 rank 的序号，其实就表明了 over(order by salary desc) 是在where age between and 后执行的

![](/imported/posts/2020-05-29-12985658-256c6c98-oracle-row-number-over-函数用法详解-分组排序-例子多/images/img_004_610ab12b57cb.png)

## 示例二

### 使用row_number（）函数进行编号

```text
select email,customerID, ROW_NUMBER() over(order by psd) as rows from QT_Customer
```

### 在订单中按价格的升序进行排序，并给每条记录进行排序代码如下：

```text
select DID,customerID,totalPrice,ROW_NUMBER() over(order by totalPrice) as rows from OP_Order
```

### 统计出每一个各户的所有订单并按每一个客户下的订单的金额 升序排序，同时给每一个客户的订单进行编号。这样就知道每个客户下几单了

```text
select ROW_NUMBER() over(partition by customerID  order by totalPrice)
 as rows,customerID,totalPrice, DID from OP_Order
```

### 统计每一个客户最近下的订单是第几次下的订单

```text
with tabs as
(
select ROW_NUMBER() over(partition by customerID  order by totalPrice)
 as rows,customerID,totalPrice, DID from OP_Order
 )
select MAX(rows) as '下单次数',customerID from tabs
group by customerID
```

### 筛选出客户第一次下的订单

思路。利用rows=1来查询客户第一次下的订单记录

```text
    with tabs as
    (
    select ROW_NUMBER() over(partition by customerID  order by insDT) as rows,* from OP_Order
    )
    select * from tabs where rows = 1
    select * from OP_Order
```

### 注意：在使用over等开窗函数时，over里头的分组及排序的执行晚于“where，group by，order by”的执行。

```text
    select
    ROW_NUMBER() over(partition by customerID  order by insDT) as rows,
    customerID,totalPrice, DID
    from OP_Order where insDT>'2011-07-22'
```
