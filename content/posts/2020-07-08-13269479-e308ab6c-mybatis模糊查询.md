{

  "title": "Mybatis模糊查询",
  "date": "2020-07-08",
  "description": "VideoMapper.java VideoMapper.xml",
  "tags": [
    "笔记"
  ],
  "source": "cnblogs-export",
  "source_url": "https://www.cnblogs.com/chenyanbin/p/13269479.html"

}

VideoMapper.java

```text
    /**
     * 根据评分和标题模糊查询
     * @param point
     * @param title
     * @return
     */
    List<Video> selectByPointAndTitleLike(@Param("point") double point,@Param("title") String title);
```

VideoMapper.xml

```text
    <select id="selectByPointAndTitleLike" resultType="net.cybclass.online.domain.Video">
select * from video where point=#{point} and title like concat('%',#{title},'%')
    </select>
```
